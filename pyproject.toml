[project]
name = "dream-rsi-local"
version = "0.1.0"
description = "Local reimplementation of the Dream-RSI loop (replay-simulator meta-exploration) on online bin packing with an Ollama model"
requires-python = ">=3.11"
dependencies = ["numpy>=1.26", "matplotlib>=3.8"]

[tool.uv]
package = false
